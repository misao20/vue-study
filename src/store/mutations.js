export default {
    SET_DATA(state, data) {
        state.data = data;
    },
    SET_USER(state, user) {
        state.user = user;
    },
    SET_ITEM(state, item) {
        state.item = item;
    }
}